extends header.pug

block content
    .container
    each set in sets
        .card(style="width: 18rem")
            img.card-img-top(src=`${set.img}` alt=`${set.name}`)
            .card-body
                p.card-text #{set.name}
